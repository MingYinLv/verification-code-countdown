var verificationCodeCoutndown = require('../src/verification-code-countdown.js');
var expect = require('chai').expect;

describe('初始化测试', function() {
    it('测试不了------------------', function() {
        expect(1+1).to.be.equal(2);
    });
});